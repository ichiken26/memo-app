{
  "name": "memoAppNuxtTraining",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "deploy:workers": "wrangler --cwd .output deploy",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test:api": "node --experimental-strip-types --test tests/api.test.ts",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "nuxt": "^4.4.5",
    "vue": "^3.5.34",
    "vue-router": "^5.0.6"
  }
}
